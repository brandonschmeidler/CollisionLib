local shapes = {
    line = require('line'),
    segment = require('segment'),
    circle = require('circle'),
    rectangle = require('rectangle'),
    orectangle = require('orectangle')
}

return shapes